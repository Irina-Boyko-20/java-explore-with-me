package ru.practicum.category.dto;

/**
 * DTO для представления категории событий.
 * <p>
 * Используется для передачи данных о категории между слоями приложения,
 * в частности - для возврата информации о категориях в API-ответах.
 * Содержит только необходимые для отображения данные.
 * </p>
 *
 * <p>
 * Является иммутабельным объектом (record), что обеспечивает потокобезопасность
 * и предсказуемость поведения. Не содержит бизнес-логики и используется
 * исключительно для передачи данных.
 * </p>
 *
 * @param id уникальный идентификатор категории
 * @param name название категории
 */
public record CategoryDto(Long id, String name) {
}
