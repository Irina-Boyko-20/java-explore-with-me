package ru.practicum.user.dto;

/**
 * DTO (Data Transfer Object) для представления пользователя.
 * <p>
 * Используется для передачи данных о пользователе между слоями приложения,
 * в частности - для возврата информации о пользователях в API-ответах.
 * Содержит основные данные пользователя, необходимые для отображения в интерфейсе.
 * </p>
 *
 * <p>
 * Является иммутабельным объектом (record), что обеспечивает потокобезопасность
 * и предсказуемость поведения. Не содержит бизнес-логики и используется
 * исключительно для передачи данных.
 * </p>
 *
 * <p>
 * Порядок параметров в record соответствует порядку объявления полей,
 * что важно для корректной работы механизмов сериализации/десериализации.
 * </p>
 *
 * @param email электронная почта пользователя
 * @param id уникальный идентификатор пользователя
 * @param name отображаемое имя пользователя
 */
public record UserDto(String email, Long id, String name) {
}
