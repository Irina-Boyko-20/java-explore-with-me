package ru.practicum.user.dto;

import ru.practicum.event.dto.EventShortDto;

/**
 * DTO (Data Transfer Object) для краткого представления пользователя.
 * <p>
 * Используется для передачи минимальной информации о пользователе в контекстах,
 * где требуется только базовая идентификация - например, при отображении
 * инициатора события или участника в сокращенном формате.
 * </p>
 *
 * <p>
 * Является иммутабельным объектом (record), что обеспечивает потокобезопасность
 * и предсказуемость поведения. Содержит только самые необходимые поля для
 * идентификации пользователя без избыточных данных.
 * </p>
 *
 * <p>
 * Часто используется как вложенный объект в других DTO таких, как {@link EventShortDto},
 * где требуется указать информацию об инициаторе события в компактном формате.
 * </p>
 *
 * @param id уникальный идентификатор пользователя
 * @param name отображаемое имя пользователя
 */
public record UserShortDto(Long id, String name) {
}
